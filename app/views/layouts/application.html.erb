<!DOCTYPE html>
<html lang="en"><head prefix="<%= yield :prefix %>">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta charset="utf-8">
    <!--<meta http-equiv="X-UA-Compatible" content="IE=edge">-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="google-site-verification" content="qKsDltnTUDqJlVfHc3aupjOiCHHY3FeeQHCVPojwY0I" />
    <% if content_for?(:meta_tags)  %>
      <%= yield :meta_tags %>
    <% else %>
       <meta name="description" content="Downloadrare collects all your favorite movies and tv shows from around the internet in a central location so that you never have to hop from one site to another looking to download your favorite movie or tv show">
    <% end %>
    
     <head>
    <%= favicon_link_tag '/favicon.ico' %>

    <title><%= yield(:title) %></title>

    <!-- Bootstrap core CSS -->
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= yield :page_css %>
    <link href="http://netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" rel="stylesheet">
	  <link href='http://fonts.googleapis.com/css?family=Oswald:400,700,300' rel='stylesheet' type='text/css'>
	  <link href='http://fonts.googleapis.com/css?family=Roboto:400,700,500,400italic,500italic,700italic' rel='stylesheet' type='text/css'>
    <%= csrf_meta_tags %>


    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
  </head>

  <body class="body-green">
	
  <!-- navbar -->
  <%= render 'layouts/navbar' %>

	<!-- Wrapper -->
	<div class="wrapper">
	
	  <!-- Topic Header / breadcrumbs -->
	  <%= yield :topic_header %>
	
	  <div class="container">
    <%= render 'shared/flash' %>   
      

	  	<%= yield %>
	  
	  </div> <!-- / .container -->
	  
	</div> <!-- / .wrapper -->
    
	<!-- Footer -->
	<%= render 'layouts/footer' %>
	
    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= yield :page_js %>
    <% if Rails.env.production? %>
      <!-- dont track when admin is signed in -->
      <% unless admin_signed_in? %>
        <%= render 'shared/google_analytics' %>
      <% end %>
    <% end %>

</body></html>